{
  "name": "cem",
  "version": "1.0.0",
  "description": "Christian Evangelism Media - Monorepo",
  "private": true,
  "scripts": {
    "dev:api": "pnpm --filter cem-api dev",
    "dev:web": "pnpm --filter cem-web dev",
    "dev:ops": "pnpm --filter cem-ops dev",
    "dev:scanner": "pnpm --filter cem-scanner dev",
    "build:api": "pnpm --filter cem-api build",
    "build:web": "pnpm --filter cem-web build",
    "build:ops": "pnpm --filter cem-ops build",
    "build:scanner": "pnpm --filter cem-scanner build",
    "build:all": "pnpm build:api && pnpm build:web && pnpm build:ops && pnpm build:scanner",
    "generate:types": "pnpm --filter cem-api generate:types"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/christian-evangelism-media/cem.git"
  },
  "keywords": [
    "evangelism",
    "media",
    "monorepo"
  ],
  "author": "",
  "license": "MIT OR Unlicense",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  }
}
