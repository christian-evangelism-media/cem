{
  "name": "cem",
  "version": "1.0.0",
  "description": "Christian Evangelism Media - Monorepo",
  "private": true,
  "scripts": {
    "dev:api": "pnpm --filter api dev",
    "dev:web": "pnpm --filter web dev",
    "dev:ops": "pnpm --filter ops dev",
    "dev:scanner": "pnpm --filter scanner dev",
    "build:api": "pnpm --filter api build",
    "build:web": "pnpm --filter web build",
    "build:ops": "pnpm --filter ops build",
    "build:scanner": "pnpm --filter scanner build",
    "build:all": "pnpm build:api && pnpm build:web && pnpm build:ops && pnpm build:scanner",
    "generate:types": "pnpm --filter api generate:types"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/christian-evangelism-media/cem.git"
  },
  "keywords": [
    "evangelism",
    "media",
    "monorepo"
  ],
  "author": "",
  "license": "MIT OR Unlicense",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  }
}
